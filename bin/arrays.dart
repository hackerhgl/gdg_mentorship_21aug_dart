void main(List<String> args) {
  final List<dynamic> list = ["a", "b", "c", 1, 2, "last"];

  final length = list.length;

  // print(length);
  // print(list[length - 1]);

  // for (var i = 0; i < length; i++) {
  //   print(list[i]);
  // }

  // for (var value in list) {
  //   print(value);
  // }

  // list.forEach((element) {
  //   print(element);
  // });
}
